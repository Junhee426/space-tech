# 서비스 확장 검토 (2026-09-13)

이 앱은 백엔드가 없는 정적 웹앱이므로 "서비스 확장"은 서버 확장이 아니라, **데이터셋이 계속 커져도(최근 업데이트마다 30~50%씩 증가) 클라이언트에서 전체를 한 번에 내려받아 파싱하는 현재 구조가 버티는가**를 뜻합니다. 이번 검토는 (1) 데이터 페이로드 크기, (2) 관계 지도·검색 인덱스의 알고리즘 복잡도, (3) `data.js → evidence-data.js → mission-data.js → mission-programs.js → research.js` 순서 의존성의 견고성 세 가지를 실측했습니다.

## 1. 현재 페이로드 실측

| 파일 | 크기 |
|---|---|
| dist/data.js | 35,754 B |
| dist/evidence-data.js | 41,741 B |
| dist/mission-data.js | 8,034 B |
| dist/mission-programs.js | 74,390 B |
| dist/research.js | 4,027 B |
| **합계 (5개 데이터 파일)** | **약 163.9 KB** |

참고로 `dist/app.js`는 40.5KB, `dist/webmcp.js`는 4.4KB, `dist/styles.css`+`dist/experience.css`는 약 25KB입니다. 데이터 122개 항목·151개 관계·59개 출처·36개 주장별 근거·24개 임무 기준입니다. gzip 전송 시 실제 다운로드량은 이보다 작지만, **파싱·실행은 압축 해제 후 전체 텍스트에 대해 일어나므로** 원본 크기가 의미 있는 지표입니다. 5개 파일은 모두 `<script defer>`로 로드되어 HTML 파싱은 막지 않지만, 첫 렌더(`app.js` 실행)는 5개 파일이 순서대로 모두 실행 완료된 뒤에 시작됩니다.

**판단:** 163KB는 현재로선 문제가 아닙니다. 그러나 `mission-programs.js`가 임무 1개당 사업 목적·역할·구성·검증·기술적 고려사항 3개 이상을 원문 인용과 함께 저장하는 구조라 **임무 수가 늘수록 가장 빠르게 커지는 파일**입니다(24개 임무에 74KB, 임무당 약 3KB). 지금 추세(버전마다 30~50% 증가)가 몇 차례 더 반복되면 수백 KB~1MB대로 갈 수 있어, 그 시점부터는 "전체를 한 파일씩 동기 로드"가 체감 로딩 지연으로 이어질 수 있습니다.

## 2. 알고리즘 복잡도 점검

`dist/research.js`(공유 검색 인덱스)와 `dist/app.js`의 관계 지도 렌더링을 직접 읽고 확인한 결과, **의미 있는 O(n²) 패턴은 발견되지 않았습니다.**

- 엔티티/출처/필드/인접 리스트(`adjacency`)는 모두 로드 시 한 번 `Map`으로 색인되어 조회는 O(1)입니다 (이전 PR "인덱스 기반 엣지 조회"에서 이미 개선됨).
- 검색(`matches`, `search`, `searchSources`)은 매 호출마다 **엔티티 수에 선형(O(n))**이며, 입력마다 재계산하지만 150ms 디바운스가 걸려 있어 문제가 되지 않습니다.
- 관계 지도(`renderMap`/`drawGraph`)는 이미 노드당 최대 8개 연결만 그리고, 연결이 8개를 넘으면 자체 페이지네이션(`graphPage`)으로 나눠 그립니다 — 즉 **개별 노드 기준으로는 이미 렌더링 규모가 상한선을 갖고 있습니다.**
- 다만 실증임무·기업/제품 카드 **목록 화면**(`missions`, `catalog`, `saved`)은 필터링된 목록 전체를 한 번에 DOM에 렌더링합니다(현재 최대 75개: 기업/기관 46 + 제품 29). 항목 수 자체가 수백 단위로 늘어나기 전까지는 문제가 되지 않지만, 페이지네이션이 전혀 없다는 점은 계속 늘어나는 데이터셋에서 지켜볼 지점입니다.

**판단:** 이번 검토에서 실제로 고쳐야 할 O(n²) 버그는 없었습니다. 목록 화면 페이지네이션은 지금 수정하지 않고 "유예 항목"으로 남깁니다 (아래 4절).

## 3. 로드 순서 의존성 — 이번에 고친 부분

`evidence-data.js`, `mission-data.js`, `mission-programs.js`는 모두 이전 파일이 `window.ATLAS`에 채워둔 값을 **그대로 신뢰하고 직접 수정**합니다. `index.html`의 `<script defer>` 순서가 지켜지는 한 정상 동작하지만, 다음 두 가지가 실제 위험이었습니다.

1. **진단 불가능한 실패**: 순서가 어긋나거나 파일 로드에 실패하면 `D.entities.find(...)` 같은 코드가 `Cannot read properties of undefined` 같은 일반적인 오류만 던져, 무엇이 왜 빠졌는지 알기 어려웠습니다.
2. **부분 손상의 무한정 전파**: 예를 들어 `evidence-data.js`가 중간에 예외로 멈추면 `D.evidence=[]`까지는 이미 실행된 상태로 남아, 이후 `mission-data.js`/`mission-programs.js`는 정상 실행되고 앱도 "정상적으로" 뜨지만 근거·별칭 등 일부 데이터가 조용히 빠진 채로 서비스될 수 있었습니다. 기존 `app.js`의 가드는 `window.ATLAS`가 아예 없을 때만 오류 화면을 보여주고, 이런 부분 손상은 잡아내지 못했습니다.

**변경 내용:**
- `evidence-data.js`, `mission-data.js`, `mission-programs.js` 각 파일 맨 앞에 "직전 파일이 실행됐는가"를 확인하는 가드를 추가하고, 실패 시 어떤 파일이 어떤 선행 파일을 필요로 하는지 명시한 `Error`를 던지도록 했습니다.
- `app.js`의 초기 점검을 `window.ATLAS`의 존재 여부뿐 아니라 `entities/sources/links/events/evidence` 배열과 **모든 임무의 `demonstration.program`이 실제로 채워졌는지**까지 확인하도록 넓혔습니다. 파이프라인이 일부만 완료된 채로 렌더링을 시작해 화면 깊숙한 곳에서 알 수 없는 오류를 내는 대신, 기존에 있던 "자료를 불러오지 못했습니다" 안내를 그대로 재사용해 안전하게 멈춥니다.
- `scripts/validate.mjs`에 이 가드들이 실제로 동작하는지 확인하는 테스트를 추가했습니다: 각 파일을 선행 파일 없이 로드하면 지정한 오류 메시지로 실패해야 하고, `mission-programs.js`는 `data.js`만으로는 부족하고 `mission-data.js`까지 필요하다는 것도 검증합니다.
- `README.md`에 로드 순서 규칙과, 새 데이터 파일을 추가할 때 지켜야 할 3단계(선행 조건 가드 추가 → `index.html` 스크립트 순서 → `validate.mjs` 순서 테스트 추가)를 명문화했습니다.

이 변경은 정상 동작 시 아무 것도 바꾸지 않고(가드는 항상 통과), 순서가 깨지는 상황에서만 동작이 달라집니다.

## 4. 페이로드 증가 조기 경보 — 이번에 추가한 부분

`scripts/validate.mjs`(빌드 명령)에 5개 데이터 파일의 합계 용량을 재는 예산 검사를 추가했습니다. 현재 약 164KB이며, 예산은 넉넉하게 360KB로 잡아 통상적인 증가는 그대로 통과시키되, 급격하거나 예상 밖의 증가는 빌드를 실패시켜 눈에 띄지 않게 계속 커지는 것을 막습니다. `validate.mjs`의 요약 JSON 출력에도 파일별·합계 용량을 추가해 배포마다 추세를 볼 수 있게 했습니다.

## 5. 유예한 항목 (이번에 손대지 않음)

- **목록 화면 페이지네이션**: `missions`/`catalog`/`saved` 화면은 여전히 필터링된 전체 목록을 한 번에 렌더링합니다. 현재 최대 75개 카드 수준에서는 체감 성능 문제가 없어 회귀 위험을 감수하면서까지 지금 손대지 않았습니다(특히 `browser-check.mjs`가 정확한 카드 개수를 단언하고 있어, 임계값을 잘못 잡으면 향후 테스트가 깨지기 쉽습니다). 항목 수가 수백 단위로 늘어나는 시점에는 "N개 초과 시 더 보기" 방식의 점진적 렌더링을 검토해야 합니다.
- **데이터 파일 지연 로딩**: `mission-programs.js`처럼 특정 화면(임무 상세)에서만 쓰는 데이터를 진입 시점에 동적으로 불러오는 구조는 이번 범위(백엔드·빌드 시스템 추가 금지)를 넘어서는 재구성이라 판단해 유예합니다. 예산 검사(4절)가 이 결정을 다시 검토해야 할 시점을 알려줄 것입니다.
- **검색 인덱스 리팩터링**: 현재 O(n) 선형 검색은 예상 성장 범위(수백 개 항목)에서 문제가 없어 그대로 유지합니다.

## 6. 검증 결과

- `node scripts/validate.mjs`: **통과** (기존 검사 전체 + 이번에 추가한 페이로드 예산·로드 순서 가드 테스트 포함, 데이터 122개 항목·59개 출처·36개 근거·24개 임무 기준)
- `ATLAS_BROWSER=/opt/pw-browsers/chromium node scripts/browser-check.mjs`: 루트 컨테이너에서 Chromium 샌드박스 제약으로 `--no-sandbox` 래퍼를 통해 실행, 1280px·390px × 기본화면/공유검색 URL **4개 시나리오 모두 통과** (앱 무결성 점검 추가 후에도 정상 동작 확인).
